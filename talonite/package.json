{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "talonite",
  "title": "Talonite",
  "version": "0.1.0",
  "description": "Two edges, one blade: pinned timezones for any instant, and a network/proxy toggle",
  "icon": "icon.png",
  "author": "jamielink",
  "license": "MIT",
  "commands": [
    {
      "name": "timezone",
      "title": "Timezones",
      "subtitle": "Talonite",
      "description": "Pinned timezones for now or any custom instant",
      "mode": "view"
    },
    {
      "name": "proxy",
      "title": "Proxy Status",
      "subtitle": "Talonite",
      "description": "Show current Wi-Fi, proxy state, host:port, and toggle each proxy",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.80.0",
    "@raycast/utils": "^1.19.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "ray lint"
  },
  "preferences": [
    {
      "name": "hideWorldMap",
      "description": "Hide the world map and show a dense list of timezones instead",
      "label": "Hide World Map",
      "type": "checkbox",
      "required": false,
      "title": "Hide World Map",
      "default": false
    }
  ]
}
