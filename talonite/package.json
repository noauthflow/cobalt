{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "talonite",
  "title": "Talonite",
  "version": "0.1.0",
  "description": "Four edges, one blade: pinned timezones, a proxy toggle, a color picker, and an on-screen ruler",
  "icon": "icon.png",
  "author": "jamielink",
  "license": "MIT",
  "commands": [
    {
      "name": "timezone",
      "title": "Timezones",
      "subtitle": "Talonite",
      "description": "Pinned timezones for now or any custom instant",
      "mode": "view"
    },
    {
      "name": "proxy",
      "title": "Proxy Status",
      "subtitle": "Talonite",
      "description": "Show current Wi-Fi, proxy state, host:port, and toggle each proxy",
      "mode": "view"
    },
    {
      "name": "color",
      "title": "Color Picker",
      "subtitle": "Talonite",
      "description": "Pick any pixel on screen and copy its color",
      "mode": "no-view",
      "preferences": [
        {
          "name": "format",
          "title": "Color Format",
          "description": "The format to copy the picked color in",
          "type": "dropdown",
          "data": [
            { "title": "Hex", "value": "hex" },
            { "title": "RGB", "value": "rgb" },
            { "title": "HSL", "value": "hsl" }
          ],
          "default": "hex",
          "required": false
        }
      ]
    },
    {
      "name": "ruler",
      "title": "Ruler",
      "subtitle": "Talonite",
      "description": "Measure the distance between two points on screen, in pixels",
      "mode": "no-view",
      "preferences": [
        {
          "name": "dragMode",
          "title": "Drag Mode",
          "description": "By default: click point A and point B to measure distance",
          "type": "checkbox",
          "label": "Drag point A to point B to measure distance",
          "default": false,
          "required": false
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.80.0",
    "@raycast/utils": "^1.19.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "native": "sh build-native.sh",
    "build": "npm run native && ray build",
    "dev": "ray develop",
    "lint": "ray lint"
  },
  "preferences": [
    {
      "name": "hideWorldMap",
      "description": "Hide the world map and show a dense list of timezones instead",
      "label": "Hide World Map",
      "type": "checkbox",
      "required": false,
      "title": "Hide World Map",
      "default": false
    }
  ]
}
